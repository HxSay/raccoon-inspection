spring:
  application:
    name: raccoon-cloud-data
  config:
    import: nacos:localhost:8848
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
  datasource:
    url: jdbc:mysql://localhost:3306/raccoon_inspection?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.raccoon.cloud.data.entity

server:
  port: 8083